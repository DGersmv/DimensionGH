# Инструкция по установке для пользователей

## 📦 Что нужно установить

При установке плагина DimensionGH_Gh на другом компьютере вам понадобятся **два файла**:

1. **DimensionGH_Gh.gha** - основной файл плагина
2. **Newtonsoft.Json.dll** - библиотека для работы с JSON (обязательна!)

## 📍 Куда копировать

Оба файла нужно скопировать в папку:
```
%APPDATA%\Grasshopper\Libraries\
```

Полный путь обычно:
```
C:\Users\ВАШЕ_ИМЯ\AppData\Roaming\Grasshopper\Libraries\
```

## ✅ Шаги установки

### Вариант 1: Автоматическая установка (если есть исходники)

Если у вас есть исходный код проекта:
1. Откройте проект в Visual Studio
2. Соберите проект (Build → Build Solution)
3. Файлы автоматически скопируются в нужную папку

### Вариант 2: Ручная установка (готовые файлы)

1. Скопируйте файл `DimensionGH_Gh.gha` в папку:
   ```
   %APPDATA%\Grasshopper\Libraries\
   ```

2. **ОБЯЗАТЕЛЬНО** скопируйте файл `Newtonsoft.Json.dll` в ту же папку:
   ```
   %APPDATA%\Grasshopper\Libraries\Newtonsoft.Json.dll
   ```

3. Перезапустите Rhino и Grasshopper

## 🔍 Где взять Newtonsoft.Json.dll?

### Если у вас есть исходники проекта:
Файл находится в папке:
```
DimensionGH_Gh\packages\Newtonsoft.Json.13.0.3\lib\net45\Newtonsoft.Json.dll
```

Или после сборки в:
```
DimensionGH_Gh\bin\Debug\Newtonsoft.Json.dll
```
или
```
DimensionGH_Gh\bin\Release\Newtonsoft.Json.dll
```

### Если у вас только готовый .gha файл:
Попросите разработчика предоставить `Newtonsoft.Json.dll` вместе с плагином.

## ⚠️ Важно!

**Без файла `Newtonsoft.Json.dll` плагин НЕ БУДЕТ РАБОТАТЬ!**

Если вы видите ошибки при загрузке плагина в Grasshopper, проверьте:
1. Есть ли файл `Newtonsoft.Json.dll` в папке `%APPDATA%\Grasshopper\Libraries\`
2. Правильная ли версия DLL (должна быть версия 13.0.3 или совместимая)

## 🧪 Проверка установки

После установки проверьте наличие файлов:

**В PowerShell:**
```powershell
$libPath = "$env:APPDATA\Grasshopper\Libraries"
Test-Path "$libPath\DimensionGH_Gh.gha"
Test-Path "$libPath\Newtonsoft.Json.dll"
```

**В командной строке:**
```cmd
dir "%APPDATA%\Grasshopper\Libraries\DimensionGH_Gh.gha"
dir "%APPDATA%\Grasshopper\Libraries\Newtonsoft.Json.dll"
```

Оба файла должны существовать!

## 📝 Рекомендация для разработчиков

При распространении плагина всегда включайте в пакет установки:
- ✅ `DimensionGH_Gh.gha`
- ✅ `Newtonsoft.Json.dll`

Это избавит пользователей от необходимости искать DLL отдельно.

